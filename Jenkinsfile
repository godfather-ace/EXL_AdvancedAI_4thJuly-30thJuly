pipeline {
    agent any
    
    stages {
        stage("Data Cleaning") {
            steps {
                echo "Pre-processing the data for ML"
            }
        }
        stage("Model Building") {
            steps {
                echo "Linear Regression Model Built"
            }
        }
        stage("Model Evaluation") {
            steps {
                echo "Models Evaluated"
            }
        }
        stage("Deploy") {
            steps {
                echo "Models Deployed"
            }
        }
    }
}
